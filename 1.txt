	Act as a Principal Backend Engineer and System Architect. I am building "PlumeTrace", an AI-powered municipal environmental forensics platform for the IEEE Smart Cities Hackathon 2026. I need a production-grade, highly reliable, and asynchronous Python backend architecture using FastAPI and MQTT to manage real-time air quality streams.

Please write the complete code implementing the following structure:

1. PROJECT ARCHITECTURE:
Create a single cohesive codebase using this exact directory layout:
/backend
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── models.py
│   ├── schemas.py
│   ├── mqtt_handler.py
│   └── websocket_manager.py
├── mock_sensors.py
├── requirements.txt
└── docker-compose.yml

2. TECHNICAL SPECIFICATIONS:

- requirements.txt: Include fastapi, uvicorn, gmqtt, pydantic, pydantic-settings, sqlalchemy[asyncio], aioredis, websockets, asyncio, paho-mqtt.
- config.py: Implement BaseSettings using pydantic-settings to manage environment variables (MQTT_BROKER_HOST, DATABASE_URL, ALLOWED_ORIGINS).
- database.py: Set up an asynchronous SQLAlchemy engine using 'sqlite+aiosqlite:///./plumetrace.db'. Provide an async context manager function `get_db_session`.
- models.py: Define a SQLAlchemy model `AirQualityReading` with fields: id (UUID primary key), sensor_id (String), timestamp (DateTime), latitude (Float), longitude (Float), pm25 (Float), so2 (Float), wind_speed (Float), wind_direction (Float). Include appropriate database indices on sensor_id and timestamp.
- schemas.py: Create Pydantic v2 validation models matching the database schema for incoming telemetry. Ensure strict type checking and validation constraints (e.g., wind_direction must be between 0 and 360).
- mqtt_handler.py: Create an asynchronous MQTT listener using a library like `gmqtt` or an async loop wrapped around `paho-mqtt`. It must subscribe to the topic 'city/airquality/+' on startup, validate payloads against Pydantic schemas, write them asynchronously to the SQLite database via `get_db_session`, and pass the validated event directly to the `websocket_manager`.
- websocket_manager.py: Implement a robust, thread-safe connection manager to track connected frontend web clients and broadcast incoming real-time telemetry to all active sockets at `/ws/plume`. Handle unexpected disconnects cleanly.
- main.py: Initialize the FastAPI application, set up CORS policies matching ALLOWED_ORIGINS, link the MQTT client to startup/shutdown event lifespans, and provide a REST endpoint GET `/api/v1/sensors/history` fetching the last 100 entries for time-series visualization.
- mock_sensors.py: A separate script that instantiates 4 virtual sensor stations located deterministically around an industrial zone. It must use an async loop to publish random but realistic sensor data with periodic toxic spikes (where SO2 exceeds safe thresholds) to the MQTT broker every 2 seconds.
- docker-compose.yml: Orchestrate an Eclipse Mosquitto MQTT broker container exposing port 1883, alongside your local dev configurations.

Provide highly modular, clean code with explicit type hints, production logging, and robust try-except error handling blocks. Avoid writing partial code snippets, comments like '# implement later', or shortcuts.