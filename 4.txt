Act as a Principal Frontend Engineer specializing in Geospatial Data Visualization and Interactive Map Platforms. I need a responsive, highly polished dashboard interface for PlumeTrace built using Next.js 14+ (App Router), TailwindCSS, and the `react-map-gl` wrapper for Mapbox GL.

Please generate the file structures and complete source code for a dark-mode command and control center dashboard using the following structure:

1. TARGET FILES:
- /frontend/src/app/page.tsx (Main dashboard page layout)
- /frontend/src/components/MapEngine.tsx (The Mapbox canvas element)
- /frontend/src/components/SidebarLogs.tsx (The analytics reporting interface)

2. COMPONENT DESIGN SPECIFICATIONS:

- layout & page.tsx: A full-bleed, 100vh interface with zero overflow scrolling. Use a grid layout dividing the screen into a wide central map pane and a fixed-width left-hand contextual controls sidebar (width: 400px) utilizing modern glassmorphism elements (backdrop-blur, border-slate-800/50, background slate-950/80).
- MapEngine.tsx:
  * Mount an interactive Mapbox viewport using a dark map style configuration asset string.
  * Establish a WebSocket connection (`ws://localhost:8000/ws/plume`) inside a React `useEffect` hook. Track incoming real-time sensor array records in state variables.
  * Map static array metrics onto the canvas as custom SVG markers. If an individual node's gas concentration passes standard safety parameters, change its marker color from a pulsing neon emerald green to a blinking red warning beacon.
  * When a user selects or clicks an individual sensor node, overlay a sleek Mapbox Popup containing real-time readouts of $PM_{2.5}$, $SO_2$, wind speed, and direction vectors.
  * Implement an animated source/layer visualization component. Add a Mapbox GeoJSON `Source` and a `Layer` (type="radar-pulse" or "heatmap"). Write a calculation function that, upon receiving an alert, traces a path directly *opposite* to the current wind direction vector, rendering an expanding, semi-transparent red plume geometry leading back to an origin icon indicating the suspected source factory.
- SidebarLogs.tsx:
  * Build an active alert terminal window tracking live telemetry logs rolling down the screen.
  * Include a prominent, styled action button labeled "Run Forensic Attribution Engine". When clicked, this button triggers an active loading skeleton spinner state, executes an asynchronous API POST request to the backend agent orchestrator, and displays the returning multi-paragraph compliance report in a clean, scrollable glass container with a button to export the report text.

Ensure all React components use proper TypeScript interfaces for all event parameters and state items, implement clean component boundaries, and avoid using any mock placeholders or partial functions.