Act as a Senior AI Research Scientist specializing in Neuro-Symbolic computing and Physics-Informed Neural Networks (PINNs). I need a complete, self-contained PyTorch training script for PlumeTrace that solves the inverse-dispersion source attribution challenge. The model must reconstruct a pollutant plume backward in time by integrating physical fluid equations into its loss function.

Please write a comprehensive, fully functional script `pinn_engine.py` addressing the following criteria:

1. THEORETICAL MATHEMATICAL BACKGROUND:
The model must solve an approximation of the 2D Advection-Diffusion partial differential equation (PDE), which governs how a pollutant spreads downwind over time:
dC/dt + u*(dC/dx) + v*(dC/dy) - D*(d^2C/dx^2 + d^2C/dy^2) = 0
Where C is concentration, (u, v) are localized wind components, and D is the turbulent diffusion coefficient. To trace data backward, our physics evaluation evaluates the residual error of this equation across a grid of collocation points.

2. ARCHITECTURAL REQUIREMENTS:
- PINN Model Class: Define a PyTorch 'nn.Module' neural network named 'PlumeInversionPINN'. It must take 3 inputs: normalized spatial features x, y, and elapsed time t. It should output a single continuous value representing Concentration (C). Use a fully connected architecture with 5 hidden layers, 64 neurons per layer, and Tanh activation functions (essential for smooth second-order derivatives).
- Custom Physics-Loss Engine: Implement a function 'compute_pde_residual(x, y, t, model, u, v, D)'. Use 'torch.autograd.grad' with 'create_graph=True' and 'retain_graph=True' to accurately compute the exact first-order and second-order partial derivatives (dC/dt, dC/dx, dC/dy, d^2C/dx^2, d^2C/dy^2). Calculate and return the mean squared residual of the PDE across the input grid.
- Training & Loss Function Core: Write a robust 'train_inversion_model' function. Total Loss must be calculated as: Total Loss = lambda_data * Data_Loss + lambda_physics * Physics_Loss.
  * Data_Loss: Mean Squared Error between the model's predicted concentration and the physical readings retrieved from our 4 mock IoT sensors.
  * Physics_Loss: The PDE residual computed over a randomly sampled grid of 2,000 spatial-temporal collocation points across our city sector.
- Coordinate Probabilistic Grid Generator: Create an evaluation function that, after training, runs inference over a dense coordinate grid (mesh-grid) covering the entire industrial park. It should output a normalized spatial probability map (a 2D numpy matrix of latitudes and longitudes) indicating the statistical likelihood of where the emission source originated.

3. DATA INTERFACE:
Include a synthetic data generation block inside the script so that it runs standalone during a hackathon demo. Generate mock spatial points, timestamps, and target pollution levels, injecting a known simulated factory source location. The training function must log optimization performance metrics (epoch count, data loss, physics loss) to console outputs every 50 epochs using an Adam optimizer. Ensure all tensors are handled cleanly with explicit device checking (CUDA, MPS, or CPU). Do not use stub code or abbreviations.